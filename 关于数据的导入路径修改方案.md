1.  你运行脚本的路径：
    `d:\code9\battery-forecasting-main\battery-forecasting-main-中文（注释）\experiments\`
2.  你数据所在的路径：
    `d:\code9\battery-forecasting-main\raw-data\`

从你运行脚本的 `experiments` 目录出发，你需要：

1.  返回 `../` (到达 `...-中文（注释）` 目录)
2.  返回 `../../` (到达 `battery-forecasting-main` 目录)
3.  然后直接进入 `raw-data/` 目录。

所以，正确的相对路径应该是 `../../raw-data/`。

然而，你的代码 `utils/exp_util.py` 和 `utils/exp_util_new.py` 正在查找的路径是 `../../data/raw-data/` (多了一个 `data` 文件夹)。

-----

### 正确的修改方案

你需要打开这两个 `utils` 文件，**删除路径中多余的 "data/"**。

#### 1\. 文件: `utils/exp_util.py`

  * **找到第144行：**
    ```python
    dir = '../../data/raw-data/{}/'.format(experiment_map[cell])
    ```
  * **修改为：**
    ```python
    dir = '../../raw-data/{}/'.format(experiment_map[cell])
    ```

#### 2\. 文件: `utils/exp_util_new.py`

  * **找到第159行：**
    ```python
    dir = '../../data/raw-data/{}/'.format(experiment_map[cell])
    ```
  * **修改为：**
    ```python
    dir = '../../raw-data/{}/'.format(experiment_map[cell])
    ```

-----

请进行这两处修改。这会使代码查找 `d:\code9\battery-forecasting-main\raw-data\`，从而正确找到你的文件并解决 `TypeError` 错误。