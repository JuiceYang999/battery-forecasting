这篇论文的核心论点是：**电化学阻抗谱（EIS）是一个信息丰富的信号，结合机器学习（特别是XGBoost），可以在电池使用模式不均匀（uneven usage）且无须了解完整历史（no knowledge of usage history）的情况下，准确预测其未来性能（如下一周期放电容量 Q_n+1）**。

### 复现标准：你要达成什么？

你的复现应被视为成功，如果你能在你的本地环境中重现以下几个关键的、可量化的结果：

1.  **核心性能复现 (对应论文 Table 1 / Fig 3)**：
    * **标准：** 必须证明 **EIS + Protocol (未来充放电行为)** 的组合是预测性能的关键。
    * **量化指标：** 你需要跑出对比实验，结果应接近：
        * `EIS + Protocol` (代码中的 `eis-actions`)：Test R² ≈ 0.83，Test Error ≈ 8.2% 。
        * `Protocol only` (代码中的 `actions`)：Test R² ≈ 0.32，Test Error ≈ 15.4%。
        * `EIS only` (代码中的 `eis`)：Test R² ≈ 0.05，Test Error ≈ 20.7%。
    * **结论：** 你的结果必须清晰地显示，组合特征的效果远大于单独的任一特征。

2.  **数据效率复现 (对应论文 Fig 5)**：
    * **标准：** 验证论文的“数据高效性”论点。
    * **量化指标：** 当训练数据仅使用 **8 个电芯（cells）** 时，模型的测试误差（Test Error）应能达到 **低于 10%**。

3.  **长期预测复现 (对应论文 Fig 4 )**：
    * **标准：** 验证模型不仅能预测“下一周期”，也能预测“未来多周期”。
    * **量化指标：** 即使在预测 **未来40个周期** (n-step lookahead = 40) 时，模型的 R² 值仍应保持在 **0.75 左右**。

4.  **鲁棒性复现 (对应论文 Fig 6 / Table 3 )**：
    * **标准：** 证明模型对“领域转移（Domain Shift）”具有鲁棒性。
    * **量化指标 (二选一或全选)：**
        * **不同使用模式 (Fig 6)：** 在“可变放电”数据上训练的模型，应用到“固定放电”数据集上，测试 R² 仍能达到 **~0.76** 。
        * **不同工作温度 (Table 3)：** 在 23°C 数据上训练的模型，应用到 35°C 数据集上，包含EIS的模型的错误率（14.6%）必须**显著低于**不包含EIS的模型（34.2% ）。

### 复现思路：你要怎么做？

基于上述标准和你的代码文件，我为你规划了具体的复现思路（执行路径）：

1.  **第一步：复现核心性能 (Table 1 )**
    * **目标：** 得到 Table 1 的核心对比数据。
    * **对应脚本：** `experiments/next-cycle-capacity.py`。
    * **操作指南：**
        1.  打开 `next-cycle-capacity.py`。
        2.  找到 `input_names = [...]` 列表。
        3.  为了复现 Table 1，你**必须**确保此列表中包含了论文中对比的关键项。根据 `utils/exp_util.py` 中的 `extract_input` 函数定义，你需要包含：
            * `'eis-actions'` (EIS + Protocol，核心)
            * `'actions'` (Protocol only)
            * `'eis'` (EIS only)
            * `'cvfs-actions'` (CVF + Protocol，论文中的SOTA对比项)
            * `'eis-cvfs-actions'` (EIS + CVF + Protocol，论文中的最佳组合)
        4.  运行此脚本。它将按照论文中描述的12折交叉验证（`n_splits':12`）和10个集成模型（`n_ensembles':10`）进行训练 ，并将结果（R²、Error）输出到日志文件 `log-next-cycle-12s.txt`。
        5.  **验收：** 检查日志文件中的结果是否与“复现标准1”中的量化指标一致。

2.  **第二步：复现数据效率 (Fig 5) 和长期预测 (Fig 4)**
    * **目标：** 验证模型的效率和远见。这两个实验可以并行进行。
    * **数据效率 (Fig 5)：**
        * **对应脚本：** `experiments/data-efficiency.py`。
        * **操作指南：** 直接运行此脚本。它会按 `n_cells_list = [2, 4, 8, 16, 20]` 循环，自动训练并记录不同训练集大小下的性能。
        * **验收：** 检查输出的 `log-n-cells.txt`，确认 `8 cells` 时的 Test Error 是否低于10%。
    * **长期预测 (Fig 4)：**
        * **对应脚本：** `experiments/n-step-lookahead.py`。
        * **操作指南：** 直接运行此脚本。它会按 `n_steps = [1, 2, 4, ..., 40]` 循环，自动训练并记录预测不同未来步长的性能。
        * **验收：** 检查输出的 `log-n-step-lookahead.txt`，确认 `step = 40` 时的 R² 是否仍在 0.75 左右。

3.  **第三步：复现鲁棒性 (Fig 6 & Table 3 )**
    * **目标：** 展示模型的泛化能力，这是汇报中的一大亮点。
    * **操作指南 (顺序执行)：**
        1.  **训练 Type2 (RS Pro) 模型：** 运行 `experiments/vd2-train.py`。这将为 Table 2的复现做准备（虽然我们主要关注 Table 1, 3 和 Fig 6）。
        2.  **测试不同放电模式 (Fig 6)：** 运行 `experiments/fixed-discharge-predict.py`。此脚本会加载**第一步**中训练好的 `variable-discharge` 模型，并在 `fixed-discharge` 数据集上测试。
        3.  **测试不同温度 (Table 3)：** 运行 `experiments/vd2-predict.py`。此脚本会加载在 `variable-discharge` (23°C 数据) 上训练的模型，并在 `vd2-35C` (35°C 数据) 上测试。
    * **验收：** 检查 `fixed-discharge-predict.py` 的输出 R² 是否接近 0.76。检查 `vd2-predict.py` 的输出（保存的 .npy 文件，你需要自己计算 R² 和 Error）是否符合“复现标准4”中的温度鲁棒性指标。

这个基调和思路为你提供了清晰的执行路径和明确的成功标准。你接下来的工作就是按照这个思路运行实验、收集结果，并为第二部分的汇报流程做准备。

等你准备好后，请上传你关于第二点（汇报流程）的资料，我将在此基础上帮你构建一个完整、系统、有深度的汇报框架。